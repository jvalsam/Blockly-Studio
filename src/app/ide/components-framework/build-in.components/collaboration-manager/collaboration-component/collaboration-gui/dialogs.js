

export function openStartSessionDialogue(
    $dialog,   // jquery selector
    $container,
    onSuccess, // cb
    onCancel   // cb
    ) {
        onSuccess({
            name: "alex"
        });
}